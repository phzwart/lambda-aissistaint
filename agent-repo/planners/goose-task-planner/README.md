# Goose Task Planner

This planner spec describes how AIssistAInt should use the managed Goose headless service as a planning engine.

The spec is read-only in the UI. Users bind its model roles to configured LiteLLM aliases and may override those bindings per project. Provider keys stay behind LiteLLM/OpenBao.
